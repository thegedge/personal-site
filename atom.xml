---
layout: nil
title: Atom Feed
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
	<id>{{ site.production_url }}</id>
	<title>{{ site.title }}</title>
	<link href="{{ site.production_url }}/atom.xml" rel="self"/>
	<link href="{{ site.production_url }}"/>
	<updated>{{ site.time | date_to_xmlschema }}</updated>
	<author>
		<name>{{ site.author.name }}</name>
		<email>{{ site.author.email }}</email>
	</author>

	{% for post in site.posts %}
	<entry>
		<id>{{ site.production_url }}{{ post.id }}</id>
		<title>{{ post.title }}</title>
		<updated>{{ post.date | date_to_xmlschema }}</updated>
		<link href="{{ site.production_url }}{{ post.url }}"/>

		<category term="{{ post.category }}"/>
		{% for tag in post.tags %}
		<category term="{{ tag }}"/>
		{% endfor %}
		
		<summary type="html">{{ post.content | split:"<!-- more -->" | first | xml_escape }}</summary>
		<content type="html">{{ post.content | remove_first:"<!-- more -->" | xml_escape }}</content>
	</entry>
	{% endfor %}
</feed>
